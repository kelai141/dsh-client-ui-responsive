# PR Gate — dsh-client-ui-responsive：构建 + 单测门禁
name: PR Gate

on:
  pull_request:
  push:
    branches: [main]

jobs:
  build-test:
    name: npm build + test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: npm test
